name = "scamalytics-checker"
main = "_worker.js"
compatibility_date = "2024-01-01"

# Account ID (Optional - you can set this later in dashboard)
# account_id = "your-account-id"

# Route settings (Optional)
# routes = [
#   { pattern = "example.com/*", zone_name = "example.com" }
# ]

# Environment variables (Optional)
# [env.production]
# name = "scamalytics-checker-production"

# KV Namespaces (Optional - for caching)
# kv_namespaces = [
#   { binding = "CACHE", id = "your-kv-id" }
# ]

# Worker settings
[build]
command = ""

# Limits
[limits]
cpu_ms = 50
